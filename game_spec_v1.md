# ゴブリン・コロニー ゲーム仕様書 v1.0

> **本書の位置づけ**: GDD v10 / known_issues / known_issues_world の設計知識を
> 「作るもの」として蒸留した実装仕様書。設計経緯・検証ログは元文書を参照すること。
> 未確定の数値は §NUMBERS に集約し、都度 §15 ルーティングで更新する。

---

## 目次

1. [コンセプト・ゴール](#1-コンセプトゴール)
2. [スコープ定義（v1.0 境界線）](#2-スコープ定義v10-境界線)
3. [確定メカニクス一覧](#3-確定メカニクス一覧)
4. [画面・UI 設計](#4-画面ui-設計)
5. [実装フェーズ計画](#5-実装フェーズ計画)
6. [未確定数値リスト（§NUMBERS）](#6-未確定数値リストnumbers)
7. [将来バージョン（v1.0 対象外）](#7-将来バージョンv10-対象外)

---

## 1. コンセプト・ゴール

### ジャンル・プラットフォーム
スマホ横持ちコロニー建築防衛シミュレーション。
Unity / Godot 等のゲームエンジンには依存しない純 TypeScript + Canvas 2D 実装。

### 1 文コンセプト
**「愚かで弱く、ちょっと愛らしいゴブリンたちを、彼らの神として指揮し、
規定日数を生き延びさせる」**

### 設計思想（変えてはならない軸）
| 軸 | 確定内容 |
|---|---|
| **前向きジレンマ** | 後ろ向き選択（使うと損する）を作らない。常に「何を優先するか」の配分で緊張を生む |
| **個体を管理させない** | ゾーンと優先度を指定し、個体はゴブリンが（愚かに）解釈する |
| **DDA 不採用** | 難度を裏で動かさない。難度は外交ルート選択（§13）の静的ダイヤル |
| **弱い個が群れて強くなる** | 頭数が戦力。個体の熟練蓄積より頭数維持が主軸 |
| **カミサマの霊的立場** | 直接ユニット操作なし。奇跡と命令で間接干渉のみ |

### 勝敗条件
- **勝利**: 規定日数を生存。最終日に特大襲撃（ラストバトル）があり、これを凌いでクリア
- **敗北**: ゴブリン全滅、またはトーテム（核）破壊

---

## 2. スコープ定義（v1.0 境界線）

### v1.0 に含む（確定）

| カテゴリ | 内容 |
|---|---|
| **コアシミュレーション** | `src/sim/` 全体（テスト全 OK 済み） |
| **描画** | Canvas 2D Renderer（状態を描くだけ・状態を持たない） |
| **プレイヤー操作** | 速度コントロール、4 ダイヤル操作、任命、奇跡 6 種、派遣 |
| **ゲームループ** | 昼夜サイクル、二層襲撃、ラストバトル |
| **ゴブリン AI** | §5 ステートマシン（実装済み）、性格、族長 |
| **資源・部屋** | 食料/建材/薬草/装備/宝石、6 種部屋 |
| **増殖・捕虜** | 求愛→つがい→出産、苗床、生贄+召喚、捕虜 4 カテゴリ |
| **外交（部分）** | 敵対度メーター、自動襲撃スケジューラ（実装済み） |
| **セーブ/ロード** | スナップショット方式（実装済み・テスト OK） |

### v1.0 に含まない（将来バージョン）

| カテゴリ | 理由 |
|---|---|
| §13 外交の双方向化（朝貢での敵対度低下・3 勢力分メーター分離） | 実装コア未接続 |
| 食料生産の増殖従属（§2.5 食料サイクル） | 数値調整が先決 |
| §14 アミナ（人間女性の通常加入） | 中立ルート条件と連動する複合システム |
| フリーモード / サンドボックスモード | 通常モード確立後 |
| Python ↔ TS parity チェック（ALL_MATCH） | cycle 層の照合ハーネス未実装 |

---

## 3. 確定メカニクス一覧

### 3-1. 信仰経済（§3）

```
祈祷 1 回 → 累計信仰 += P（減らない・ランク判定用）
           → 信仰残高  += P（奇跡の燃料）

奇跡発動  → 信仰残高  -= C（累計に影響しない）
```

- 残高には上限（ランク連動で上昇）。上限超え分は**累計にのみ積まれ捨てない**
- トーテムランクが上がると奇跡性能・シャーマン枠・残高上限が上昇
- シャーマン枠は「上限」であって「強制でない」——頭数比例の控えめ配分が既定（KI-03）

### 3-2. 奇跡 6 種（§4）

| 名称 | 意図スロット | 効果概要 |
|---|---|---|
| 恵みのパン虫 | 維持・強化（平時/面的） | 弱い敵生物が多数出現。食料補給 + 微経験値 |
| 嘲りの稲妻 | 敵を減らす（直接） | 対象に直接ダメージ |
| 名誉ある死 | 博打/捨て身 | 対象ゴブリンが恐怖なしで死ぬまで戦う |
| 泥の抱擁 | 今すぐ凌ぐ（防御） | 地点に一時壁を生成し侵入路を塞ぐ |
| 抑えられない怒り | 敵を乱す（間接） | 範囲内の敵を一定時間同士討ちさせる |
| 下僕召喚 | 欠員補充（戦時/点的） | 指定地点にゴブリン 1 体を即出現。重コスト |

> **ラストバトルの主戦場は泥の抱擁＋抑えられない怒り**。
> 特大襲撃は頭数で殴り勝てないため、範囲奇跡の質で受ける設計（KI-06 確定）。

### 3-3. ゴブリン AI ステート（§5、優先度順）

1. 死亡（終端）
2. 激昂（恐怖なし・一定時間で解除）
3. 恐怖（敵から離れる・安全確認ベースで解除）
4. 戦闘（HP 減 → 恐怖へ）
5. 瀕死（寝床へ移動）
6. 空腹（食物を探す）
7. 睡眠（寝床へ）
8. 仕事（ジョブキュー + 性格重み）
9. 放浪（採集・軽事故・稀に事故死）

- **事故死はステートマシン外の独立レイヤー**。HP を経由しない離散イベント
- **族長は恐怖なし**（唯一の明示例外）

### 3-4. 戦闘・防衛（§8/§9）

- **戦力は雄が担う**。雌は恐怖閾値が高く自動的に後退（産み手の保護）
- **族長**: 群れの中心。周囲頭数に比例した HP ボーナス。前に出ず「狙われやすい」設計
- **ユニーク個体の瀕死保護（族長・アミナ共通）**: HP0 で即死せず倒れる → 仲間が搬送できれば復帰
- **巣口デフォルト 3 本**。3 本未満には設定不可（出口潰し対策）
- **防衛ライン**: スライダーで頭数配分。敵の進軍方向を予兆フェーズで明示
- 戦闘中のプレイヤー操作は奇跡のみ

### 3-5. ゲームループ 1 日（§11）

```
昼: 採集・建築・祈り・欲求充足・外征（§11.5）
夜: 活動低下、就寝。外征不可（締め切り）
  └ 二層襲撃が発生しうる
日が変わるごとに敵の規模が強化
最終日: 特大ラストバトル → 凌げばクリア
```

**二層襲撃**:
- **大規模（脅威）**: 敵対度連動で 1〜5 日に 1 回。フル防衛フロー
- **小規模（恵み）**: 1 日 0〜1 回（確率）。準自動解決。間接報酬（食料/捕虜）のみ。頭数直接増加なし

### 3-6. 増殖と捕虜（§2.5）

**増殖フロー**:
```
雌が起点で求愛 → 相手と寝床へ → 確定妊娠 → 1 日で出産（一腹 1〜6）
→ 子は 1 日で成体
妊娠中は追加妊娠不可。出産直後から再び可能（高回転）
```

- **雌律速**: 「雌の数 × 出産サイクル」が増殖上限を自然に決める
- **損耗時バフ（必須骨格）**: 直近損耗割合が閾値超で妊娠率乗算。数日で減衰（KI-04 確定）
- **巣立ち**: 頭数上限超過が平時に続くと超過分が巣口から去る（安全弁）
- **二段収束**: 日常死亡（確率的減衰）+ 巣立ち（決定的キャップ）で青天井・全滅の両方を防ぐ

**捕虜 4 カテゴリ**:

| カテゴリ | 主な出口 |
|---|---|
| 雌ゴブリン捕虜 | 苗床（最高価値・温存） |
| 雄ゴブリン捕虜 | 即加入（成長ラグなし）/ 安い生贄（女の半額） |
| 人間捕虜（中立ルートなし） | 生贄 / 売却 / 追放 |
| 人間捕虜（中立ルートあり） | 保持 / 解放のみ |

**苗床**: 雌捕虜を配置し確定生産。人間母体は高速だが消耗も速い（KI-23 確定）

### 3-7. 外交・敵対度（§13 部分実装）

- **敵対度メーター**: 0（和平）〜1（MAX）
- **上昇**: 人間母体の苗床産 / 人間捕虜の生贄
- **下降**: 人間捕虜の解放（控えめ）
- **敵対度 → 大規模襲撃間隔**: 0 → 5 日、MAX → 1 日
- 朝貢による敵対度低下・3 勢力メーター分離は v1.0 外

### 3-8. 部屋システム（§7）

| 部屋 | 種別 | 効果 |
|---|---|---|
| ネズミ牧場 | 生産 | 食料（単機能・高効率） |
| キノコ農園 | 生産 | 食料 + 薬草。遅いが多用途 |
| 泥鍛冶屋 | 生産 | 装備製造（資源消費） |
| 苗床部屋 | 強化 | 妊娠率底上げ。雌捕虜で効率アップ |
| まじない医の部屋 | 役職 | 平時: 負傷者治療加速 / 戦時: 後衛回復+遠距離攻撃 |
| 族長の間 | 役職（ユニーク） | 族長 1 体を任命 |
| トーテム | 役職（ユニーク） | シャーマン任命（ランク連動で枠増加）、奇跡・ランクアップの核 |

### 3-9. セーブ/ロード（§14.5.1 確定）

- スナップショット方式（往復バイト一致テスト OK）
- 保存対象: 全 Goblin 配列、WorldState、RNG 内部状態（4-tuple）
- タイミング: バックグラウンド移行、日境界、襲撃フェーズの切れ目
- 実時間をロジックに持ち込まない。保持は `tick` / `day` のみ

---

## 4. 画面・UI 設計

### 4-1. 画面ゾーニング（横持ち 3 層）

```
┌─────────────── ステータスバー（上端） ────────────────┐
│  経過日/規定日  |  信仰残高ゲージ  |  頭数  |  主要資源  │
├────────────────────────────────────────────────────────┤
│                                                          │
│               マ ッ プ（中央・メイン）                   │
│                                                          │
├────────────────────────────────────────────────────────┤
│ ダイヤル（左下） │  ミニカード列（下端中央） │ 速度+通知（右下）│
└─────────────────────────────────────────────────────────┘
```

**ステータスバー（上端）**
- 経過日数 / 規定日数、信仰残高ゲージ、累計信仰、頭数（現在/上限）
- 敵対度: 最も高い 1 勢力を警告色で端に表示。タップで 3 勢力詳細パネル

**ダイヤル（左下・4 分割）**
- 資源 / 建築 / 撤去 / 派遣
- 任命はダイヤルに常駐しない → 建物タップ → サブメニューで割り当て

**ミニカード列（下端）**
- ゴブリン 1 体 = 顔アイコン + 緊急度バー（緑/黄/赤）
- タップ → カメラが当該個体へパン
- 同時表示 5〜12 体（画面幅上限）

**速度コントロール（右下）**
- 停止 / 1x / 3x
- 通知（階層化）:
  - 大規模襲撃予兆 → 強い割り込み（要対応）
  - 小規模襲撃 → 軽いトースト
  - 巣立ち / 関係性イベント → 原因が読める軽通知 + マップピン

### 4-2. 操作文法（統一）

> **すべて「対象を指定して確定」。タップの意味は選択中モードで固定**

| モード | 操作 |
|---|---|
| 資源 | 指でなぞった範囲を一括指定。採掘か伐採かはゴブリンが自動判別 |
| 建築 | ゴースト配置 → 2 タップ確定 |
| 撤去 | 独立ダイヤル（誤操作防止）。モード中タップ = 撤去で固定 |
| 派遣 | 出現物タップ → 頭数スライダー → 確定 |
| 任命 | 建物タップ → サブメニュー（シャーマン/族長/まじない医/苗床） |
| 奇跡 | 奇跡ボタン → 対象タップ（または範囲ドラッグ） |
| 関係性イベント | 通知タップ → 許可/拒否 の 2 択（引き離しは追加確認を挟む） |

### 4-3. Renderer 設計方針

- **状態を持たない純粋 Renderer**。`render(worldState, params) → void`
- Canvas 2D を基本。個体数増で重くなれば PixiJS へ移行
- `src/browser_entry.ts` 経由で `globalThis.GoblinSim` にコアを載せる（既存設計）

---

## 5. 実装フェーズ計画

### フェーズ 0（前提・完了済み）
シミュレーションコアの確立。4 スイート全 OK の状態。

```
✅ src/sim/ 全体（rng / params / cycle / goblin / state_machine /
               tick_driver / world_state / world_params / world）
✅ parity/ 4 スイート（SNAPSHOT_ROUNDTRIP_OK / STATEMACHINE_OK /
                     TICKDRIVER_OK / WORLD_OK）
✅ KI-22〜25（reproduction / 人間母体苗床 / 敵対度 / 二層襲撃）
```

---

### フェーズ 1「プロトタイプ版」

**目的**: ゲームループが動いている画面を見る。UI は最低限。数値調整の土台を作る。

**完了条件（Go 基準）**:
- Canvas に WorldState が描画される（個体位置・HP・ステート色）
- 速度コントロール（停止/1x/3x）が動く
- ラストバトルを含む通しプレイが完走する
- ブラウザで `goblin_colony_dashboard.html` を開いて動作確認できる

**タスクリスト**:

```
P1-01  Canvas 2D Renderer の骨格（状態なし・純粋関数）
         - WorldState を受け取り個体・巣口・壁を描く
         - 個体: 座標・ステート色（緑/黄/赤）・HP バー
         - ループ: requestAnimationFrame → tickDriver → render

P1-02  速度コントロール UI
         - 停止/1x/3x ボタン → tickDriver.setSpeed()
         - 現在速度の表示

P1-03  ゲームループ終端の実装
         - 全滅検知 → ゲームオーバー画面
         - 規定日数達成 → 勝利画面（特大ラストバトル後）
         - 日境界でのオートセーブ呼び出し

P1-04  ラストバトル（特大襲撃）のトリガ
         - 最終日に FINAL_MULT 倍の襲撃を発火
         - 撃退でクリア・壊滅で敗北

P1-05  build_dashboard.mjs の動作確認
         - esbuild インストール確認
         - バンドル → HTML 出力 → 動作確認
```

**プロトタイプ版の意図的な省略**:
- 操作 UI（ダイヤル・任命・奇跡）は一切なし
- オートプレイヤー（AI が自動で補充・配分）で走らせる
- ステータスバー・ミニカード列なし

---

### フェーズ 2「テスト版（内部 QA）」

**目的**: プレイヤー操作を通してゲームループが成立するかを内部で確認する。
「ゴブリンを管理させない」という体験が操作として成立しているかを検証する。

**完了条件（Go 基準）**:
- 操作なしの自動プレイと、操作ありのプレイで難度・生存率に明確な差がある
- §15 の調整ダイヤル（一腹分布・敵対度係数・損耗バフ）を触って辛勝レンジに乗れる
- 速度 3x でも 30 日通しプレイが安定してクラッシュしない

**タスクリスト**:

```
P2-01  ステータスバー実装
         - 経過日数/規定日数、信仰残高ゲージ、頭数、主要資源

P2-02  ダイヤル UI（4 分割）
         - 資源モード: なぞりで採集指示
         - 建築モード: ゴースト → 確定
         - 撤去モード
         - 派遣モード（§11.5 外征）

P2-03  任命 UI
         - 建物タップ → サブメニュー（族長 / シャーマン / まじない医 / 苗床）

P2-04  奇跡 UI（6 種）
         - 奇跡ボタン → 対象タップ / 範囲ドラッグ
         - 信仰残高の消費・不足時フィードバック

P2-05  通知システム（階層化）
         - 大規模襲撃予兆（強割り込み）
         - 小規模襲撃（トースト）
         - 巣立ち・関係性イベント（軽通知 + マップピン）

P2-06  ミニカード列
         - 個体カード（緑/黄/赤）・タップでカメラパン

P2-07  防衛ライン + 配分スライダー
         - 巣口 3 本のデフォルト配置
         - 予兆フェーズで配分スライダー表示

P2-08  §15 数値調整ハーネス
         - パラメータを URL クエリストリング or JSON で上書きできる
         - 多シード自動プレイヤーで辛勝レンジを計測するスクリプト

P2-09  パフォーマンス計測
         - 速度 3x / 個体 40 体 / 60fps 維持を確認
         - 問題があれば PixiJS 移行検討
```

---

### フェーズ 3「本実装（β）」

**目的**: v1.0 スコープを全て実装し、バランス調整済みの通常モードを完成させる。

**タスクリスト**:

```
P3-01  §15 実数調整の完了
         - 一腹分布・人間母体倍率・敵対度係数・小規模報酬・
           苗床/召喚コスト・補充閾値をまとめてチューニング
         - 目標: 辛勝レンジ（全滅もせず楽勝もしない帯）が
           多シード・多難度で成立

P3-02  外征 §11.5 の完全実装
         - 出現物（採取/狩猟/戦闘）・3 系ロジック
         - 距離・昼長の連動（KI-08 確定仕様）
         - 帰還 tick・捕虜取得

P3-03  食料生産の増殖従属（§2.5 食料サイクル）
         - 食料不足 → 妊娠率低下・流産
         - 食料過剰 → 増殖バフ（foodBuff、既存フック利用）

P3-04  信仰のトーテムランク連動（§3）
         - 累計信仰でランク上昇
         - ランク → シャーマン枠・残高上限・奇跡性能・必要巣口下限

P3-05  宝石・貢ぎ物資源（§7/§13）
         - 宝石の採掘・戦利品取得
         - 保有量 → 人間敵対度への上乗せ（閾値型）
         - 売却・朝貢による敵対度低下（外交の双方向化）

P3-06  ブリーチング（後半敵の壁破壊）
         - 特定壁の破壊予告表示
         - 壁破壊ロジック

P3-07  ゲームオーバー / クリア演出
         - トーテム破壊演出
         - ラストバトルクリア演出

P3-08  通常モード難度設定画面
         - 開始時の外交ルート選択（4 ルート = 難度ダイヤル）
         - 規定日数の選択

P3-09  最終 QA・バランス確認
         - 各難度で辛勝レンジに乗ることを多シードで確認
         - クラッシュ・スナップショット往復の最終確認
```

---

### フェーズ間の Go 判断基準まとめ

| フェーズ | 次に進む条件 |
|---|---|
| P0 → P1 | 現在完了済み |
| P1 → P2 | 通しプレイが完走し、ブラウザで描画が確認できる |
| P2 → P3 | 操作ありプレイで辛勝レンジに乗れる・速度 3x でクラッシュなし |
| P3 → リリース | §15 調整完了・多難度で安定帯確認・QA 通過 |

---

## 6. 未確定数値リスト（§NUMBERS）

> 多シード自動プレイヤー（P2-08）で測定後に埋める。安易に変えると KI-22/25 の
> 相互作用が崩れるため、変更は必ず安定帯の再測定を伴うこと。

| パラメータ | 暫定値 | 調整方針 |
|---|---|---|
| 一腹分布 weights | 30/30/18/12/6/4% | 中央値 2 前後を保ちつつ高回転感を調整 |
| 人間母体倍率 `humanNurseryYieldFactor` | 2.0 | 速度ダイヤル。総量に効かない（KI-23） |
| 損耗バフ係数・閾値・減衰日数 | 要計測 | 辛勝帯の長さの主ダイヤル |
| 敵対度係数（苗床産/生贄あたり） | 要計測 | 「急拡張 vs 平和」の前向きトレードオフを成立させる |
| 小規模報酬量・発生確率 | 0.5/day 前後 | 低難度は厚く・高難度は損耗寄り |
| 苗床産出 `nurseryYieldPerCaptive` | 要計測 | ラグを埋めるが青天井にしない範囲 |
| 召喚コスト `summonCost` | 要計測 | 常用抑制が必要 |
| 求愛基礎確率 `courtBaseChance` | 要計測 | 繁殖強さと増殖ラグのバランス |
| ラストバトル倍率 `FINAL_MULT` | 要計測 | 備えていれば勝てる・備えなければ負ける |
| 大規模間隔（和平〜MAX） | 5 日〜1 日 | KI-05/08 検証済み範囲 |

---

## 7. 将来バージョン（v1.0 対象外）

| 機能 | 根拠 |
|---|---|
| **§13 外交の完全実装**（3 勢力メーター分離・朝貢・4 エンド） | v1.0 でベースを確立後 |
| **§14 アミナ**（人間女性の加入・中立ルート最終形） | 外交システム完成が前提 |
| **フリーモード / サンドボックスモード** | 通常モード完成後 |
| **Python ↔ TS parity チェック** | `parity/rng.py` 実装が先決 |
| **PixiJS 移行** | P2-09 パフォーマンス計測で必要と判断した場合 |
| **個体関係性の可視化**（系譜ビュー等） | UI 余裕があれば検討 |
| **音響・エフェクト** | 描画確立後 |

---

## 付録: アーキテクチャ早見表

```
src/sim/          ← コア（純粋関数・副作用なし）
  rng.ts              決定的 PRNG（xorshift128）
  params.ts           力学定数の単一の真実源（KI-01）
  cycle.ts            マクロ集計層（日単位）
  goblin.ts           個体型
  state_machine.ts    個体 1 体・1 tick の遷移
  world_state.ts      World 層の状態スキーマ
  world_params.ts     日次 → tick 変換（レートを再定義しない）
  world.ts            全個体 1 tick 進める + 戦闘・出生・事故死

src/renderer/     ← 描画層（P1 で新規追加）
  renderer.ts         render(worldState, params) → void

src/browser_entry.ts  globalThis.GoblinSim に載せる薄い再エクスポート

viz/
  dashboard_template.html  <!--CORE_BUNDLE--> プレースホルダ
  goblin_colony_dashboard.html  build_dashboard.mjs が生成

parity/           ← テスト（変更しない）
build_dashboard.mjs
```

**守るべき不変条件**（変えてはならない）:
- `Math.random` 禁止。RNG は `SimState.rng` のみ
- 全状態を tick の関数として閉じる（KI-09）
- 力学定数は `params.ts` が単一の真実源（KI-01）
- step は入力 state を破壊しない（純粋関数）
- 難度を裏で動かすフックを作らない（KI-10）
