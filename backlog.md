# 残タスク一覧（バックログ）

作成: 2026-06-12（`feature_gap_audit.md` の三面照合監査から、PR #19 完了分を差し引いた残り）
完成の定義: game_spec_v1 の **v1.0（P3 完了）**。本体は Godot 版、TS コアは検証済み力学のリファレンス
（力学先行: バランスに触る新力学は TS で検証 → Godot へ移植、空間依存の力学は Godot 直）。

凡例: 難易度 ★★★=高（設計とUI/力学にまたがる新規設計）/ ★★=中（確定仕様の移植・拡張）/ ★=低（独立小タスク）

## 完了済み（PR #19）

- [x] 機能ギャップ監査（GDD/spec × TS × Godot 三面照合）→ `feature_gap_audit.md`
- [x] Python↔TS パリティ照合ハーネス（`parity:check` = ALL_MATCH）
- [x] 捕虜パネル UI（TS ダッシュボード: 解放/側室/朝貢）
- [x] 奇跡 6 種 + 集合命令 + トーテムランク連動（Godot §3/§4。`test_miracles.gd`）
- [x] §13 外交の双方向化 — TS コア（3 勢力分離・朝貢・常時の業・勢力抽選）
- [x] 捕虜プール + 敵対度 + 襲撃間隔接続 — Godot 第一増分（KI-17/23/24。`test_captives.gd`）
- [x] `GoblinExt` 死んだ型 import 修正

---

## A. 高難度 ★★★（設計レビュー必須・メインセッション級）

- [x] **A1. 汎用ジョブキュー + 建築システム** — 完了（`test_jobs.gd` = JOBS_OK。
  資源スカラー 6 種 + Job{MINE/BUILD/REPAIR/DIG} + 整合スイープ一元化 + 採掘トグル +
  建築モード（ゴースト→2タップ確定→建材消費→完了で rooms[]）+ 壁修復 + ジョブ可視化。
  Haul/Pray/Craft/Guard は既存系が担うため型なし（spec 3-12 注記準拠）。
  範囲なぞり指定・撤去ダイヤルは B7 に残置。wood/herb の供給経路は A4/B6 で接続）
- [x] **A1+. 巣穴拡張（掘削）+ 建材経済** — 完了（JobType.DIG。素の壁→床化 +
  建材副産物 1.0、内側のみ＝外殻を破らない安全規則。タップで採掘/掘削/修復を
  自動解釈 §10）。**建材 3 階層**: ①鉱脈採掘（4 + 稀に宝石・有限 3 個＝序盤ブースト）
  ②掘削（壁 1 枚 +1・労働律速の持続供給＆拡張）③外征 廃墟（建材塊 + 枝＝A4 で接続）。
  掘削収量・速度は §15/D1 調整対象（今は拡張が自己資金化する控えめ設定）。
- [x] **A2. ブリーチング + 防衛配分 UI** — 完了（`test_defense.gd` = DEFENSE_OK。
  壁破壊役（breacher_from_day 以降・決定的選定）+ 破壊予告 breach_warnings（派生値）+
  警告ハイライト、無敵壁（マップ縁 + トーテム至近）、DefensePoint（巣口内側 2 タイル）+
  配分スライダー + 敵戦力比例の自動追従、破られた跡の再建（§9 復興）。
  装備取得フローは B8、襲撃フェーズの「装備→配分」の段は B8/B7 と接続。
  `WALL_HP=20` / `wall_damage_per_day` は §NUMBERS 暫定。）
- [ ] **A3. 中立ルート状態機械 + 4 ルート選択**（§14.5.7・P3-08）
  3 フラグ（is_captive / harm_committed / amina_breeding_unlocked）、加害判定ハンドラの一元化
  （UI でなく状態で出口を弾く）、難度設定画面（開始時 易/並/難 + 外交 4 ルート + 規定日数）。
  TS の humanHostility 無ドリフト設計（PR #19）が土台。
- [ ] **A4. 外征の完全実装**（§11.5・P3-02）
  出現物の残り 5 系（動物/旅人/ゴブリン放浪者=外部加入/敵性キャンプ/廃墟 + 行き倒れの少女フック）、
  距離属性（近い/遠い）、戦闘系外征（出発前に泥鍛冶屋で装備→人数で成否→返り討ちは負傷止まり）、
  難度目安表示、業の漏れ（§13 接続）、遅れて来る増援の防衛ライン自動合流（現状は即時解除の P1 簡易版）。
  外交資源（宝石・毛皮 →B5）と外部加入の供給源を兼ねる合流点。

## B. 中難度 ★★（確定仕様の移植・拡張。サブエージェント分担向き）

- [x] **B1. 3 勢力外交の Godot 移植** — 完了（`test_factions.gd` = FACTIONS_OK。
  メーター 2 本 + ドリフト + 朝貢 + TRIBUTE コマンド + 勢力抽選 + max 間隔 +
  HUD の最怒勢力表示。タップで 3 勢力詳細パネルは B7 に残置）
- [x] **B2. 捕虜系 第二増分（Godot）** — 完了（test_captives.gd 拡張 = CAPTIVES_OK。
  苗床確定生産（NURSERY 部屋必須・人間母体 ×2 + 敵対度上乗せ・母体消耗）、
  take_concubine/pending_bond/approve_bond/tear_apart_bond、捕虜パネル UI
  （生贄/解放/側室/朝貢 3 勢力）+ つがい承認バナー。A1 の建築と接続済み =
  苗床部屋を建てると稼働。副産物: 泥壁/トーテム破壊時の床キャッシュ未再構築
  という潜在 KI-09 バグを発見・修正（test_miracles の復元後決定性で検出））
- [x] **B3. 食料従属** — 完了（TS: `foodAvailable` 実接続 + 在庫/頭数で求愛抑制/
  バフ + 不足流産。world_test 5 検証 = WORLD_OK。Godot: 同係数を `_step_breeding`
  へ接続、test_food に単調性 + 流産検証 = FOOD_OK）。動力学は保守的（不足は事故/
  襲撃損耗時のみ・過剰バフ +0.15）で安定帯を崩さない設計。**実数は D1 へ申し送り**
- [x] **B4. 医療 / まじない医** — Godot 版完了（§6/spec 3-17）。`Role.WITCH_DOCTOR` を配線
  （平時は `_work_target` で WITCH 部屋付近に配置。CHIEF/SHAMAN と同様の扱い）。新設
  `_step_medic()`: 平時は寝床 (NEST) で休息中の負傷個体の HP 回復を herb 消費つきで
  加速（herb=0 なら素の hp_regen のみ＝キノコ農園との経済従属）。交戦中は防衛ライン
  (`_defense_points`) から `medic_backline_offset`(3) タイルさらに後方の `_medic_points`
  へ退避（`_medic_slot`。前衛の白兵には出ない / `assigned_to_combat` から除外）し、被ダメ
  20% 低下（`medic_dmg_reduce`）+ 周囲 (チェビシェフ3) の負傷者を遠距離治療。効果は控えめ
  (D1 調整前提)。test_medic.gd = MEDIC_OK、既存スイート全緑、6 シード勝率 6/6 (回帰なし。
  WITCH_DOCTOR は任命制のため未任命時は完全に無変化)。TS 側 (`src/sim/`) は enum 宣言のみで
  未着手のまま。
- [ ] **B5. 宝石・貢ぎ物資源**（§7・§13・§14.5.8・P3-05）: gems/毛皮/魚/色鮮やかなもの、
  宝石保有→人間敵対度上乗せ（閾値型の両刃）、売却、宝石献上、朝貢の対価を捕虜から資源へ拡張。
  供給は外征（A4）に一本化
- [x] **B6. 部屋の残り 4 種の配線**（§7）— 完了（`test_workshops.gd` = WORKSHOPS_OK）。
  キノコ農園→薬草 / 泥鍛冶屋→装備（稼働個体数 × 日次レート・建て置きは不生産）、
  建てた工房を日境界で自動配員（§10。牧場と _in_workshop で排他）。苗床=B2 済・
  族長/シャーマン=役職任命済みのため新規は工房 2 種。個別任命 UI は B7。
- [ ] **B7. P2 UI 群**（§10）: 4 分割ダイヤル（資源/建築/撤去は A1 依存。派遣は実装済み）、
  任命 UI（建物タップ→サブメニュー: シャーマン/族長/呪医/苗床）、ミニカード列（顔 + 緊急度 +
  カメラパン）、通知の階層化（強割り込み/トースト/軽通知 + マップピン。現状フィードのみ）
- [x] **B8. 装備経済**（§14.5.6・spec 3-16）— 完了（`test_workshops.gd` 内）。
  泥鍛冶屋で製造（B6）・共有在庫・襲撃開始で戦闘員が在庫から武装（在庫尽きで素手）・
  攻撃 +30%（既存）・襲撃終了で消耗・死亡で消滅。取得は襲撃開始のスカラー付与
  （spec 3-11 思想。泥鍛冶屋タイルへの移動動線・1〜2 段階・外征出発前取得は将来の
  肉付け）。消耗率は §15/D1 ダイヤル。
- [x] **B9. ユニークの自動搬送**（spec 3-21）— 完了（`test_carry.gd` = CARRY_OK）。
  `carrying_id`、最寄り非戦闘個体の担ぎ手自動選定（1 人）、搬送中は戦闘割当ブロック +
  最寄り NEST へ移動、寝床到達で降ろして回復開始。既存 downed_ticks 猶予と整合
  （降ろした tick でリセット → 回復で起立）。snapshot 往復 + 復元後決定性も検証。
- [ ] **B10. ラストバトルの波状期 + 備え放出**（§11）: 最終数日の間隔短縮（下限 2 日）、
  総力戦の備え放出（召喚連打/苗床バースト/生贄補給は奇跡・捕虜実装済みなので接続のみ）
- [ ] **B11. 個体成長（戦闘微経験値）**（§5）: 緩やか・不可視寄りの制約付き。§15 でレンジ線引き
- [ ] **B12. オンボーディング**（§14.5.2）: 初期盤面の spec 値突き合わせ（5 体・ランク 1・上限 8 前後）、
  助走窓、文脈駆動チュートリアル（統一文法 5 種を平和な数日に 1 回ずつ）

## C. 低難度 ★（独立タスク・即発注可能）

- [x] **C1. 自動セーブ** — 完了（`test_save.gd` = SAVE_OK）。JSON 型正規化（rooms/
  forage_regrow/jobs の int フィールド）+ `user://autosave.json` 保存/復元 +
  タイミング（日境界・襲撃終了のみ、交戦中は不保存、勝敗で削除）。
  ⚠ **設計上の限界（記録）**: Godot の JSON/var_to_str はどちらも任意 double のテキスト
  往復をバイト一致できない（17 桁出力を parse が 16 桁へ丸める。微小 hostility 等で非冪等）。
  KI-09 のバイト一致はライブ dict 往復（parity 側）で担保済みのため、autosave は
  「ロード後に自己無矛盾で決定的（再ロードで同じ未来を再現）」を保証する設計とした。
  バックグラウンド移行セーブは Godot の `NOTIFICATION_APPLICATION_PAUSED` 配線が要る
  （モバイル実機。未配線 = 将来）。
- [ ] **C2. AutoController の牧場雌補充の矛盾修正**: 雌を ASSIGN_ROOM →日境界の
  `_rebalance_ranch` が剥がすデッドロック疑い（PR #19 監査所見）。バランスに触るため §15 と同時に
- [ ] **C3. §15 調整インフラ**（P2-08・§14.5.4）: パラメータの JSON/URL 上書きハーネス、
  テレメトリスキーマ（襲撃ごとのログ）、多シード夜間バッチの自動化（現状 `test_seeds.gd` 手動 6 シード）
- [ ] **C4. パフォーマンス計測**（P2-09）: 3x × 個体 40 体 × 60fps。問題があれば A* キャッシュ/PixiJS 検討
- [ ] **C5. TS World 層へのランク連動移植**: Godot で実装済みの faith_cap/shaman_slots/miracle_mult
  相当を TS World 層へ（cycle 層にはあり。ダッシュボードの整合用。優先度低 = Godot が本体のため）

## D. 仕上げ（C3 完了後・リリース前）

- [ ] **D1. §15 実数調整**（P3-01）: 一腹分布 / 人間母体倍率 / 敵対度係数（3 勢力ドリフト・朝貢量含む）/
  小規模報酬 / 苗床・召喚コスト / 補充閾値 / 族長盾係数 / `WALL_HP` / ランク閾値・奇跡コスト /
  **食料従属の閾値・係数（B3）** / **A2 の breacher 数・壁破壊レート・防衛配分**。
  KI-22/25/26 の相互作用が論点。終了基準: 中道ルートが多シードで「辛勝レンジ」に乗る。
  ⚠ **申し送り（要調整）**: A2 で防衛ラインを巣口の隘路迎撃へ変更した結果、`test_seeds.gd`
  の AutoController なし勝率が 4/6 → **6/6**（comfortable wins）へ易化した。breacher 追加
  （難化）と隘路迎撃（易化）の差し引きで易化側。構造は GDD §9 どおりで正。数値（raid 規模・
  breacher_every・wall_damage_per_day・totem_panic_radius）でテンションを戻すのは D1 で一括。
- [ ] **D2. 勝敗演出 + 最終 QA**（P3-07/09）: トーテム破壊演出・クリア演出、各難度の辛勝確認、
  スナップショット往復の最終確認

## v1.0 非ブロッカー（将来バージョン・着手しない）

アミナ一式（§14。A3/A4 が前提）/ フリー・サンドボックスモード / PixiJS 移行 / 系譜ビュー /
音響 / 部族間ゼロサム（三すくみ化）/ 乱婚以外の繁殖形態（女王制等）/ 巣口の建築増減・ランク連動下限 /
美形化ロア / TS シムへの昼夜導入（Godot が本体のため不要）

## 推奨着手順（依存関係）

```
A1a 資源スカラー ─→ A1 ジョブキュー+建築 ─→ B6 部屋 ─→ B8 装備 ─→ A2 防衛/ブリーチング
B1 3勢力移植(独立) → B2 捕虜第二増分 → A3 中立ルート+難度選択
A4 外征 ─→ B5 宝石・貢ぎ物（A3 の 4 ルートと合流）
B3 食料従属 / B4 医療 / B9 搬送 / B10 波状期 / C1〜C4 は独立に並行可
最後に C3 → D1 実数調整 → D2 QA
```
